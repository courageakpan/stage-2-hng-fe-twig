[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "zip"]

[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "cp -r vendor public/ || true"
]

[start]
cmd = "cd public && php -S 0.0.0.0:${PORT:-8000} -t ."