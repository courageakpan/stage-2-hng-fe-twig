<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TicketPro</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      .clip-path-wave {
        clip-path: polygon(0 75%, 100% 55%, 100% 100%, 0 100%);
      }
    </style>
  </head>

  <body class="min-h-screen bg-gray-100 text-gray-800 relative">
    <div class="max-w-[1440px] mx-auto min-h-screen">
    <!-- Decorative Circles -->
    <div class="absolute top-10 -left-10 w-40 h-40 bg-blue-200 rounded-full opacity-60 z-0"></div>
    <div class="absolute bottom-20 -right-10 w-56 h-56 bg-blue-300 rounded-full opacity-50 z-0"></div>

<!-- Header -->
{% if page == 'landing' %}
<header class="bg-white shadow-sm py-4 px-6 md:px-10 flex justify-between items-center relative z-10">
  <h1 class="text-xl sm:text-2xl font-bold text-blue-600">TicketPro</h1>
  <nav class="hidden sm:flex space-x-4 sm:space-x-6">
    <a href="?page=login" class="text-gray-700 hover:text-blue-600 font-medium">Login</a>
    <a href="?page=signup" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">Get Started</a>
  </nav>
  <button class="sm:hidden text-blue-600 text-xl font-bold" id="mobileMenuBtn">☰</button>
</header>

<div id="mobileMenu" class="hidden absolute top-full left-0 w-full bg-white shadow-md flex flex-col space-y-2 p-4 sm:hidden z-20">
  <a href="?page=signup" class="py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">Get Started</a>
  <a href="?page=login" class="py-2 text-left text-gray-700 hover:text-blue-600">Login</a>
</div>
{% endif %}

<!-- Landing -->
{% if page == 'landing' %}
<section class="flex flex-col items-center justify-center text-center px-6 py-24 sm:py-32 md:py-40 relative overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-b from-blue-50 to-white clip-path-wave z-0"></div>
  <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-800 mb-6 relative z-10">
    Manage Support Tickets Easily
  </h2>
  <p class="text-gray-600 mb-10 relative z-10 max-w-xl text-sm sm:text-base">
    Streamline your ticket workflow with TicketPro. Create, manage, and resolve tickets efficiently.
  </p>
  <div class="relative z-10 flex flex-col sm:flex-row gap-4">
    <a href="?page=signup" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition w-full sm:w-auto">Sign Up Free</a>
    <a href="?page=login" class="bg-gray-200 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-300 transition w-full sm:w-auto">Login</a>
  </div>
</section>
{% endif %}

<!-- Login -->
{% if page == 'login' %}
<section class="flex items-center justify-center px-4 py-16 sm:py-20">
  <div class="bg-white shadow-lg rounded-xl p-6 sm:p-8 w-full max-w-sm sm:max-w-md">
    <h2 class="text-xl sm:text-2xl font-bold text-center mb-6">Login</h2>
    {% if success %}
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">{{ success }}</div>
    {% endif %}
    {% if error %}
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">{{ error }}</div>
    {% endif %}
    <form method="post" class="space-y-4">
      <input type="email" name="email" placeholder="Email" required class="w-full border rounded-lg p-3 focus:ring-2 focus:ring-blue-500" />
      <input type="password" name="password" placeholder="Password" required class="w-full border rounded-lg p-3 focus:ring-2 focus:ring-blue-500" />
      <button type="submit" class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 transition">Login</button>
    </form>
    <div class="mt-4 p-3 bg-gray-50 rounded-lg text-sm">
      <p class="font-semibold text-gray-700 mb-2">🔐 First time here?</p>
      <p class="text-gray-600 text-xs mb-3">Create your own account to get started with TicketPro. It's quick and easy!</p>
      <div class="bg-blue-50 border border-blue-200 rounded p-2 mb-3">
        <p class="text-xs text-blue-700"><strong>💡 Tip:</strong> New users get full access to create and manage tickets</p>
      </div>
      <details class="mt-2">
        <summary class="cursor-pointer text-xs text-gray-500 hover:text-gray-700">🔧 Demo accounts (for testing)</summary>
        <div class="mt-2 p-2 bg-white rounded border">
          <ul class="space-y-1 text-xs text-gray-600">
            <li><strong>Admin:</strong> john@example.com / password</li>
            <li><strong>Agent:</strong> jane@example.com / password</li>
            <li><strong>User:</strong> tom@example.com / password</li>
          </ul>
        </div>
      </details>
    </div>
    <p class="text-center text-sm text-gray-500 mt-4">
      Don't have an account?
      <a href="/signup" class="text-blue-600 hover:underline">Sign up</a>
    </p>
  </div>
</section>
{% endif %}

<!-- Signup -->
{% if page == 'signup' %}
<section class="flex items-center justify-center px-4 py-16 sm:py-20">
  <div class="bg-white shadow-lg rounded-xl p-6 sm:p-8 w-full max-w-sm sm:max-w-md">
    <div class="text-center mb-6">
      <h2 class="text-xl sm:text-2xl font-bold text-gray-800 mb-2">Create Your Account</h2>
      <p class="text-sm text-gray-600">Join TicketPro and start managing tickets efficiently</p>
    </div>
    
    <div class="mb-4 p-3 bg-green-50 border border-green-200 rounded-lg">
      <p class="text-xs text-green-700">✅ <strong>Free forever</strong> - No credit card required</p>
    </div>
    
    {% if error %}
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">{{ error }}</div>
    {% endif %}
    
    <form method="post" class="space-y-4">
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Full Name</label>
        <input type="text" name="name" placeholder="Enter your full name" required class="w-full border rounded-lg p-3 focus:ring-2 focus:ring-blue-500" />
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
        <input type="email" name="email" placeholder="you@example.com" required class="w-full border rounded-lg p-3 focus:ring-2 focus:ring-blue-500" />
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Password</label>
        <input type="password" name="password" placeholder="Create a strong password" required class="w-full border rounded-lg p-3 focus:ring-2 focus:ring-blue-500" />
        <p class="text-xs text-gray-500 mt-1">Use 8+ characters for better security</p>
      </div>
      <button type="submit" class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 transition font-medium">Create Account</button>
    </form>
    
    <div class="mt-6 p-3 bg-blue-50 rounded-lg text-xs">
      <p class="font-semibold text-blue-800 mb-1">🎯 What you get:</p>
      <ul class="space-y-1 text-blue-700">
        <li>• Create and manage unlimited tickets</li>
        <li>• Assign tickets to team members</li>
        <li>• Track ticket status and priority</li>
        <li>• Real-time dashboard analytics</li>
      </ul>
    </div>
    
    <p class="text-center text-sm text-gray-500 mt-4">
      Already have an account?
      <a href="?page=login" class="text-blue-600 hover:underline font-medium">Login here</a>
    </p>
  </div>
</section>
{% endif %}

<!-- Dashboard & Tickets -->
{% if page == 'dashboard' or page == 'tickets' %}
<div class="flex flex-col md:flex-row min-h-screen relative z-10">
  <!-- Sidebar -->
  <aside class="w-full md:w-64 bg-white shadow-md flex flex-col md:h-auto order-last md:order-first">
    <div class="p-4 md:p-6 border-b flex justify-between items-center md:block">
      <h1 class="text-xl md:text-2xl font-bold text-blue-600">TicketPro</h1>
      <button class="md:hidden text-gray-600 text-xl" id="sidebarToggle">☰</button>
    </div>
    <nav id="sidebarNav" class="hidden md:block flex-1 p-4 space-y-2">
      <a href="?page=dashboard" class="block w-full text-left py-2 px-4 rounded-lg font-medium {{ page == 'dashboard' ? 'bg-blue-50 text-blue-600' : 'hover:bg-gray-100' }}">Dashboard</a>
      <a href="?page=tickets" class="block w-full text-left py-2 px-4 rounded-lg font-medium {{ page == 'tickets' ? 'bg-blue-50 text-blue-600' : 'hover:bg-gray-100' }}">Tickets</a>
    </nav>
    <div class="p-4 border-t">
      <a href="?page=landing" class="w-full py-2 block text-center bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">Logout</a>
    </div>
  </aside>

  <!-- Main -->
  <main class="flex-1 flex flex-col">
    <header class="bg-white shadow-sm py-4 px-6 sm:px-8 flex justify-between items-center">
      <h2 class="text-lg sm:text-xl font-semibold capitalize">{{ page }}</h2>
      <nav class="hidden sm:flex space-x-6">
        <a href="?page=tickets" class="text-gray-700 hover:text-blue-600 font-medium">Tickets</a>
        <a href="#" class="text-gray-700 hover:text-blue-600 font-medium">Settings</a>
      </nav>
    </header>

    {% if page == 'dashboard' %}
    <section class="p-6 sm:p-8 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
      <div class="bg-white rounded-xl shadow-md p-6 border-l-4 border-blue-500">
        <h3 class="text-gray-500 text-sm mb-2">Total Tickets</h3>
        <p class="text-3xl font-bold">{{ stats.total ?? 0 }}</p>
      </div>
      <div class="bg-white rounded-xl shadow-md p-6 border-l-4 border-green-500">
        <h3 class="text-gray-500 text-sm mb-2">Open Tickets</h3>
        <p class="text-3xl font-bold">{{ stats.open ?? 0 }}</p>
      </div>
      <div class="bg-white rounded-xl shadow-md p-6 border-l-4 border-yellow-500">
        <h3 class="text-gray-500 text-sm mb-2">In Progress</h3>
        <p class="text-3xl font-bold">{{ stats.in_progress ?? 0 }}</p>
      </div>
      <div class="bg-white rounded-xl shadow-md p-6 border-l-4 border-gray-400">
        <h3 class="text-gray-500 text-sm mb-2">Closed Tickets</h3>
        <p class="text-3xl font-bold">{{ stats.closed ?? 0 }}</p>
      </div>
    </section>
    {% endif %}

    {% if page == 'tickets' %}
    <!-- =========================
         REPLACED TABLE WITH CARDS
         (Client-side mock CRUD)
         ========================= -->
    <section class="p-6 sm:p-8">
      <div class="flex justify-between items-center mb-6">
        <h3 class="text-lg font-semibold">Manage Tickets</h3>
        <div class="flex items-center gap-3">
          <!-- Filter by status (client-side only) -->
          <select id="filterStatus" class="border rounded p-2 text-sm">
            <option value="">All Statuses</option>
            <option>Open</option>
            <option>In Progress</option>
            <option>Closed</option>
          </select>
          <button onclick="openTicketModal()" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
            + New Ticket
          </button>
        </div>
      </div>

      <!-- Toast placeholder -->
      <div id="toast" class="fixed right-6 top-6 z-50 hidden"></div>

      <!-- Cards container -->
      <div id="cardsGrid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <!-- Cards will be rendered by JS -->
      </div>

      <!-- no-tickets message -->
      <p id="noTicketsMsg" class="text-gray-600 text-center py-10 hidden">No tickets yet. Create one using the button above!</p>
    </section>
    {% endif %}
  </main>
</div>
{% endif %}

<!-- Footer -->
{% if page == 'landing' %}
<footer class="bg-white py-6 text-center text-gray-500 mt-16 relative z-10 border-t">
  <p>© {{ "now"|date("Y") }} TicketPro. All rights reserved.</p>
</footer>
{% endif %}

<!-- New Ticket Modal -->
<div id="ticketModal" class="hidden fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full z-50">
  <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
    <div class="mt-3">
      <h3 class="text-lg font-medium text-gray-900 mb-4">Create New Ticket</h3>
      <form id="createForm" class="space-y-4" onsubmit="return handleCreate(event)">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Subject</label>
          <input type="text" id="createSubject" name="subject" required class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500">
          <p id="errSubject" class="text-xs text-red-600 hidden mt-1">Subject is required.</p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Description</label>
          <textarea id="createDescription" name="description" rows="3" required class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500"></textarea>
          <p id="errDescription" class="text-xs text-red-600 hidden mt-1">Description is required.</p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Priority</label>
          <select id="createPriority" name="priority" class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500">
            <option value="">Select priority</option>
            <option value="Low">Low</option>
            <option value="Medium" selected>Medium</option>
            <option value="High">High</option>
            <option value="Critical">Critical</option>
          </select>
          <p id="errPriority" class="text-xs text-red-600 hidden mt-1">Priority is required.</p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Assign To</label>
          <select id="createAssignedTo" name="assigned_to" class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500">
            <option value="">Unassigned</option>
            {% for agent in agents %}
            <option value="{{ agent.name }}">{{ agent.name }}</option>
            {% endfor %}
          </select>
        </div>
        <div class="flex justify-end space-x-3">
          <button type="button" onclick="closeTicketModal()" class="bg-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-400 transition">Cancel</button>
          <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">Create Ticket</button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- Edit Ticket Modal -->
<div id="editModal" class="hidden fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full z-50">
  <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
    <div class="mt-3">
      <h3 class="text-lg font-medium text-gray-900 mb-4">Edit Ticket</h3>
      <form id="editForm" class="space-y-4" onsubmit="return handleEdit(event)">
        <input type="hidden" id="editId">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Subject</label>
          <input type="text" id="editSubject" name="subject" required class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500">
          <p id="errEditSubject" class="text-xs text-red-600 hidden mt-1">Subject is required.</p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Description</label>
          <textarea id="editDescription" name="description" rows="3" required class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500"></textarea>
          <p id="errEditDescription" class="text-xs text-red-600 hidden mt-1">Description is required.</p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Priority</label>
          <select id="editPriority" name="priority" class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500">
            <option value="Low">Low</option>
            <option value="Medium">Medium</option>
            <option value="High">High</option>
            <option value="Critical">Critical</option>
          </select>
          <p id="errEditPriority" class="text-xs text-red-600 hidden mt-1">Priority is required.</p>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Status</label>
          <select id="editStatus" name="status" class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500">
            <option value="Open">Open</option>
            <option value="In Progress">In Progress</option>
            <option value="Closed">Closed</option>
            <option value="Resolved">Resolved</option>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Assign To</label>
          <select id="editAssignedTo" name="assigned_to" class="w-full border rounded-lg p-2 focus:ring-2 focus:ring-blue-500">
            <option value="">Unassigned</option>
            {% for agent in agents %}
            <option value="{{ agent.name }}">{{ agent.name }}</option>
            {% endfor %}
          </select>
        </div>
        <div class="flex justify-end space-x-3">
          <button type="button" onclick="closeEditModal()" class="bg-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-400 transition">Cancel</button>
          <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">Update Ticket</button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- Delete Confirmation Modal -->
<div id="deleteModal" class="hidden fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full z-50">
  <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
    <div class="mt-3 text-center">
      <svg class="w-16 h-16 mx-auto text-red-500 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h.638a2 2 0 002 2v10a2 2 0 002-2h-4.638a2 2 0 00-2-2V9a2 2 0 00-2-2h-4.638z"></path>
      </svg>
      <h3 class="text-lg font-medium text-gray-900 mb-2">Delete Ticket</h3>
      <p class="text-gray-600 mb-4">Are you sure you want to delete this ticket? This action cannot be undone.</p>
      <p class="text-sm font-medium text-gray-800 mb-6" id="deleteTicketName"></p>
      <div class="flex justify-center space-x-3">
        <button onclick="closeDeleteModal()" class="bg-gray-300 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-400 transition">Cancel</button>
        <button id="confirmDeleteBtn" class="bg-red-600 text-white px-6 py-3 rounded-lg hover:bg-red-700 transition">Delete Ticket</button>
      </div>
    </div>
  </div>
</div>

<script>
  // Convert initial PHP/Twig tickets to JS structure
  const initialTickets = {{ tickets|json_encode()|raw }} || [];

  // Local client-side state
  let tickets = initialTickets.map(t => ({
    id: t.id ?? (Math.floor(Math.random()*1000000)),
    subject: t.subject ?? '',
    description: t.description ?? '',
    priority: t.priority ?? 'Medium',
    status: t.status ?? 'Open',
    assigned_to: t.assigned_to ?? (t.assignee ?? t.assignee_name ?? ''),
    date: t.date ?? (t.created_at ? t.created_at.split(' ')[0] : (new Date()).toISOString().slice(0,10))
  }));

  // Utility: show toast
  function showToast(message, type = 'success') {
    const toast = document.getElementById('toast');
    toast.className = '';
    toast.classList.add('fixed','right-6','top-6','z-50','px-4','py-2','rounded-lg','shadow-lg','text-sm');
    toast.classList.add(type === 'error' ? 'bg-red-600' : 'bg-green-600');
    toast.textContent = message;
    toast.classList.remove('hidden');
    setTimeout(() => {
      toast.classList.add('hidden');
    }, 3000);
  }

  // Render tickets as cards
  function renderTickets(filterStatus = '') {
    const grid = document.getElementById('cardsGrid');
    const noMsg = document.getElementById('noTicketsMsg');
    grid.innerHTML = '';

    const filtered = filterStatus ? tickets.filter(t => t.status === filterStatus) : tickets;

    if (filtered.length === 0) {
      noMsg.classList.remove('hidden');
    } else {
      noMsg.classList.add('hidden');
    }

    filtered.forEach(t => {
      const card = document.createElement('div');
      card.className = 'bg-white rounded-xl shadow-md p-5 hover:shadow-lg transition border border-gray-100 relative';
      card.id = 'ticket-' + t.id;

      const statusBadge = (() => {
        if (t.status === 'Open') return '<span class="px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-700">'+t.status+'</span>';
        if (t.status === 'In Progress') return '<span class="px-3 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-700">'+t.status+'</span>';
        if (t.status === 'Closed') return '<span class="px-3 py-1 rounded-full text-xs font-medium bg-gray-200 text-gray-700">'+t.status+'</span>';
        return '<span class="px-3 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-700">'+t.status+'</span>';
      })();

      card.innerHTML = `
        <div class="flex justify-between items-start">
          <h3 class="text-lg font-semibold text-gray-800">${escapeHtml(t.subject)}</h3>
          ${statusBadge}
        </div>
        <p class="text-sm text-gray-600 mt-2">${escapeHtml(t.description)}</p>
        <div class="flex justify-between items-center mt-4 text-sm text-gray-500">
          <p><span class="font-semibold text-gray-700">Priority:</span> ${escapeHtml(t.priority)}</p>
          <p><span class="font-semibold text-gray-700">Assignee:</span> ${escapeHtml(t.assigned_to || 'Unassigned')}</p>
        </div>
        <div class="flex justify-between mt-5">
          <button onclick="openEditModal('${t.id}')" class="text-blue-600 hover:underline text-sm font-medium">Edit</button>
          <button onclick="openConfirmDelete('${t.id}')" class="text-red-600 hover:underline text-sm font-medium">Delete</button>
        </div>
      `;

      grid.appendChild(card);
    });
  }

  // Escape helper to prevent inline-HTML issues
  function escapeHtml(text) {
    if (text === null || text === undefined) return '';
    return String(text)
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;')
      .replace(/'/g, '&#039;');
  }

  // Initial render
  renderTickets();

  // Filter logic
  document.getElementById('filterStatus')?.addEventListener('change', function() {
    renderTickets(this.value);
  });

  // Modal helpers
  function openTicketModal() {
    document.getElementById('createForm').reset();
    hideCreateErrors();
    document.getElementById('ticketModal').classList.remove('hidden');
  }
  function closeTicketModal() {
    document.getElementById('ticketModal').classList.add('hidden');
  }

  function openEditModal(id) {
    const t = tickets.find(x => String(x.id) === String(id));
    if (!t) return showToast('Ticket not found', 'error');
    document.getElementById('editId').value = t.id;
    document.getElementById('editSubject').value = t.subject;
    document.getElementById('editDescription').value = t.description;
    document.getElementById('editPriority').value = t.priority;
    document.getElementById('editStatus').value = t.status;
    document.getElementById('editAssignedTo').value = t.assigned_to;
    hideEditErrors();
    document.getElementById('editModal').classList.remove('hidden');
  }
  function closeEditModal() {
    document.getElementById('editModal').classList.add('hidden');
  }

  // Delete confirm modal
  let deleteId = null;
  function openConfirmDelete(id) {
    const t = tickets.find(x => String(x.id) === String(id));
    if (!t) return showToast('Ticket not found', 'error');
    deleteId = id;
    document.getElementById('deleteTicketName').textContent = t.subject;
    document.getElementById('deleteModal').classList.remove('hidden');
  }
  function closeDeleteModal() {
    deleteId = null;
    document.getElementById('deleteModal').classList.add('hidden');
  }
  document.getElementById('confirmDeleteBtn')?.addEventListener('click', function() {
    if (!deleteId) return;
    tickets = tickets.filter(x => String(x.id) !== String(deleteId));
    renderTickets(document.getElementById('filterStatus')?.value || '');
    showToast('Ticket deleted.');
    closeDeleteModal();
  });

  // Create handler with client-side validation
  function showCreateErrors(subjectErr, descErr, prioErr) {
    const es = document.getElementById('errSubject');
    const ed = document.getElementById('errDescription');
    const ep = document.getElementById('errPriority');
    es.classList.toggle('hidden', !subjectErr);
    ed.classList.toggle('hidden', !descErr);
    ep.classList.toggle('hidden', !prioErr);
  }
  function hideCreateErrors() {
    showCreateErrors(false,false,false);
  }

  function handleCreate(e) {
    e.preventDefault();
    const subject = document.getElementById('createSubject').value.trim();
    const description = document.getElementById('createDescription').value.trim();
    const priority = document.getElementById('createPriority').value;
    const assigned = document.getElementById('createAssignedTo').value;

    const hasSubjectErr = !subject;
    const hasDescErr = !description;
    const hasPrioErr = !priority;

    showCreateErrors(hasSubjectErr, hasDescErr, hasPrioErr);
    if (hasSubjectErr || hasDescErr || hasPrioErr) {
      showToast('Please correct the highlighted fields.', 'error');
      return false;
    }

    // Create new client-side ticket
    const newTicket = {
      id: String(Math.floor(Math.random()*1000000000)),
      subject,
      description,
      priority,
      status: 'Open',
      assigned_to: assigned,
      date: (new Date()).toISOString().slice(0,10)
    };
    tickets.unshift(newTicket); // show newest first
    renderTickets(document.getElementById('filterStatus')?.value || '');
    closeTicketModal();
    showToast('Ticket created successfully.');
    return false;
  }

  // Edit handler with validation
  function showEditErrors(sErr, dErr, pErr) {
    document.getElementById('errEditSubject').classList.toggle('hidden', !sErr);
    document.getElementById('errEditDescription').classList.toggle('hidden', !dErr);
    document.getElementById('errEditPriority').classList.toggle('hidden', !pErr);
  }
  function hideEditErrors() { showEditErrors(false,false,false); }

  function handleEdit(e) {
    e.preventDefault();
    const id = document.getElementById('editId').value;
    const subject = document.getElementById('editSubject').value.trim();
    const description = document.getElementById('editDescription').value.trim();
    const priority = document.getElementById('editPriority').value;
    const status = document.getElementById('editStatus').value;
    const assigned = document.getElementById('editAssignedTo').value;

    const sErr = !subject;
    const dErr = !description;
    const pErr = !priority;
    showEditErrors(sErr,dErr,pErr);
    if (sErr || dErr || pErr) {
      showToast('Please correct the highlighted fields.', 'error');
      return false;
    }

    const idx = tickets.findIndex(x => String(x.id) === String(id));
    if (idx === -1) {
      showToast('Ticket not found', 'error');
      return false;
    }

    tickets[idx].subject = subject;
    tickets[idx].description = description;
    tickets[idx].priority = priority;
    tickets[idx].status = status;
    tickets[idx].assigned_to = assigned;
    tickets[idx].date = (new Date()).toISOString().slice(0,10);

    renderTickets(document.getElementById('filterStatus')?.value || '');
    closeEditModal();
    showToast('Ticket updated successfully.');
    return false;
  }

  // Hide toast after 3s (already handled in showToast)
  // Mobile menu and sidebar toggles (existing)
  document.getElementById('mobileMenuBtn')?.addEventListener('click', () => {
    const menu = document.getElementById('mobileMenu');
    if (menu) menu.classList.toggle('hidden');
  });
  document.getElementById('sidebarToggle')?.addEventListener('click', () => {
    const nav = document.getElementById('sidebarNav');
    if (nav) nav.classList.toggle('hidden');
  });

  // Ensure initial UI state (show no-tickets message if empty)
  (function init() {
    renderTickets();
  })();
</script>
    </div>
  </body>
</html>
